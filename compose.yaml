services:
  

  database:
    image: postgres:14
    env_file:
      - db.env

    volumes:
      - db_data:/var/lib/postgresql/data
    
  go:
    image: my_go_image
    build: .
    ports: 
    - 9000:9000
    command: ["/api", "${STORAGE_TYPE:-memmory}"]
    
    
    

volumes:
  db_data: